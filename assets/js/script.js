$(document).ready(function() {
    // Get latitude and longitude

    // API call to Dark Sky with coordinates

    // Retrieve and render weather data

    // Show Skycons with weather conditions

    // Unit conversion button

}) // End doc ready